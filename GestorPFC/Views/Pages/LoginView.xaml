<Page x:Class="GestorPFC.Views.Pages.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestorPFC.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginView">

            <Grid Background="#f4f4f4">
                <Border Width="300" Padding="20" Background="White" CornerRadius="10"
                VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Iniciar Sesión" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                        <TextBlock Text="Usuario:" />
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="250"/>

                        <TextBlock Text="Contraseña:" Margin="0,10,0,0"/>
                        <PasswordBox x:Name="PasswordBox" Width="250"/>

                        <Button Content="Iniciar Sesión" Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"
                        Background="DodgerBlue" Foreground="White" 
                        Margin="0,10,0,0"/>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>


            </Grid>
</Page>
