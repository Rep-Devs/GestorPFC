<Page x:Class="GestorPFC.Views.Pages.RegisterView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestorPFC.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:Background="White"
      Title="RegisterView">
    <Grid>
        <StackPanel Margin="30">
            <TextBlock Text="Registro de Usuario" FontSize="20" FontWeight="Bold"/>

            <TextBlock Text="Usuario:" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

            <TextBlock Text="Contraseña:" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" Width="200"/>

            <TextBlock Text="Rol:" Margin="0,10,0,0"/>
            <ComboBox SelectedItem="{Binding Role}" Width="200">
                <ComboBoxItem Content="Alumno"/>
                <ComboBoxItem Content="Profesor"/>
                <ComboBoxItem Content="Administrador"/>
            </ComboBox>

            <Button Content="Registrar" Command="{Binding RegisterCommand}" Margin="0,20,0,0"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Page>
